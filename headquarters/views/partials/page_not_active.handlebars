<div class="card" data-playthrough-id='{{playthrough.id}}'>
  <div class="card-header">
    <strong>
      {{playthrough.departureName}} {{playthrough.title}}
    </strong>
    {{pageset.title}} {{pagesetDisabledStart}}
    {{#unless isFirst}}
    <a href='' data-playthrough-id='{{playthrough.id}}' data-page-name='{{page.name}}' data-ui='show-pageset'>Show now</a>
    {{/unless}}
  </div>
  <div class="card-body" data-playthrough-id='{{playthrough.id}}' data-page-name="{{page.name}}" style="display: {{#if isFirst}}block{{else}}none{{/if}};">
    <p class='card-text'>
      {{pagesetDisabledIntro}}
    </p>
    <p class='card-text'>
      <div class='alert alert-warning'>
        {{pagesetDisabledMessage}}
      </div>
    </p>
  </div>
</div>
