{
  "name": "fptcore",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "scripts": {
    "watch-docs": "nodemon cmd/dump-reference.ts",
    "build-docs": "ts-node cmd/dump-reference.ts",
    "pretest": "tsc -p tsconfig.check.json --noEmit && eslint . || echo 'Lint failed.'",
    "test": "mocha --require ts-node/register --recursive 'test/**/*.ts'"
  },
  "main": "./src/index.ts",
  "dependencies": {
    "@types/node": "^22.13.10",
    "jsonschema": "^1.5.0",
    "moment": "^2.24.0",
    "moment-timezone": "^0.5.25",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "eslint": "^5.1.0",
    "mocha": "^4.1.0",
    "nodemon": "^2.0.3",
    "sinon": "^4.1.5"
  }
}
