import { connect } from 'react-redux';

import PublicLoggedOut from '../components/PublicLoggedOut';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(mapStateToProps, mapDispatchToProps)(PublicLoggedOut);
