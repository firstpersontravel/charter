import { connect } from 'react-redux';

import ScriptVersionIndex from '../components/ScriptVersionIndex';

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(mapStateToProps, mapDispatchToProps)(ScriptVersionIndex);
