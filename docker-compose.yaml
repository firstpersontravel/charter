version: '2'
services:
  web:
    image: fpt:latest
    expose: [80]
    ports: ["5000:80"]
    # uncomment all volumes to test baked version for deployment
    volumes:
      - ./static:/var/app/static
      - ./build:/var/app/build
      - ./apps/agency/static:/var/app/apps/agency/static
      - ./apps/travel/dist:/var/app/apps/travel/dist
      - ./docker/web/nginx.conf:/etc/nginx/nginx.conf
      - ./docker/web/conf:/etc/nginx/conf.d
    command: ["nginx", "-g", "daemon off;"]
    links: [server, pubsub]

  server:
    image: fpt:latest
    # uncomment all volumes to test baked version for deployment
    volumes:
      - ./apps/fptcore:/var/npm/beta/node_modules/fptcore
      - ./headquarters:/var/app/headquarters
    env_file: [./secrets/env-local]
    environment:
      # uncomment these if you want to test beta with local node modules
      - NODE_PATH=/var/npm/beta/node_modules
      - PATH=/var/npm/beta/node_modules/.bin:$PATH
    working_dir: /var/app/headquarters
    command: "nodemon --legacy-watch --polling-interval 1000 bin/server.js | pino -c"
    links: [mysql]

  worker:
    image: fpt:latest
    # uncomment all volumes to test baked version for deployment
    volumes:
      - ./apps/fptcore:/var/npm/beta/node_modules/fptcore
      - ./headquarters:/var/app/headquarters
    env_file: [./secrets/env-local]
    environment:
      # uncomment these if you want to test beta with local node modules
      - NODE_PATH=/var/npm/beta/node_modules
      - PATH=/var/npm/beta/node_modules/.bin:$PATH
    working_dir: /var/app/headquarters
    command: "nodemon --legacy-watch --polling-interval 1000 bin/worker.js | pino -c"
    links: [mysql]

  pubsub:
    image: fpt:latest
    # uncomment all volumes to test baked version for deployment
    volumes:
      - ./apps/fptcore:/var/npm/beta/node_modules/fptcore
      - ./headquarters:/var/app/headquarters
    env_file: [./secrets/env-local]
    environment:
      # uncomment these if you want to test beta with local node modules
      - NODE_PATH=/var/npm/beta/node_modules
      - PATH=/var/npm/beta/node_modules/.bin:$PATH
    working_dir: /var/app/headquarters
    command: "node bin/pubsub.js | pino -c"

  mysql:
    image: mysql:5.7
    environment: [MYSQL_ROOT_PASSWORD=root]
    volumes: ["./docker/mysql:/docker-entrypoint-initdb.d"]
    ports: ["4306:3306"]
